"""Integration tests against live services land phase-by-phase."""

import pytest


@pytest.mark.integration
def test_placeholder() -> None:
    pytest.skip("Integration tests will be added per phase.")
